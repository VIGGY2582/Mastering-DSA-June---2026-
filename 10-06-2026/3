// 3691. Maximum Total Subarray Value II

class Solution {
    public long maxTotalValue(int[] nums, int k) {
    int max = nums[0];
    int min = nums[0];
    for(int i : nums){
        max= Math.max(max,i);
        min = Math.max(min,i);
    }
    return (long) (max-min)*k;
        
    }
}